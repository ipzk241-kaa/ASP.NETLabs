@model CoursesApp.Models.Course
<div class="col">
    <div class="card h-100 shadow-sm">
        <div class="card-body">
            <h5 class="card-title">@Model.Title</h5>
            <p class="card-text">@Model.Description</p>
            <p class="text-muted">Ціна: <strong>@Model.Price</strong> грн</p>
        </div>
    </div>
</div>